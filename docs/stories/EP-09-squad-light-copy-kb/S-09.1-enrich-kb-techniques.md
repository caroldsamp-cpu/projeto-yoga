# S-09.1 - Enriquecer KB com Extração de Técnicas via LLM

**Epic:** EP-09 - Squad Light Copy KB
**Story ID:** S-09.1
**Tipo:** Data Engineering / Pipeline
**Prioridade:** MUST
**Estimativa:** 8 pontos
**Status:** [ ] Nao Iniciada
**Dependencias:** squad light-copy-kb (normalização concluída — 3.581 chunks em `data/normalized-chunks.jsonl`)

---

## User Story

**Como** desenvolvedor do squad light-copy-kb,
**Quero** que os 3.581 chunks normalizados tenham os campos `techniques`, `literary_elements` e `persuasion_drivers` preenchidos via classificação LLM,
**Para que** o retrieval do agente de rewrite seja preciso e rastreável, citando técnicas de Light Copy com base no conteúdo real de cada chunk.

---

## Contexto

O pipeline de normalização de transcrições foi concluído e produziu 3.581 chunks estruturados em `squads/light-copy-kb/data/normalized-chunks.jsonl`. Contudo, uma validação de qualidade revelou que **82% dos chunks (2.924/3.581) estão com o campo `techniques` vazio**, e 100% estão sem `literary_elements` e `persuasion_drivers`.

O script `normalize-transcripts.js` usa heurísticas simples (matching de keywords) que não capturam a semântica real do texto. A solução é um script de enriquecimento LLM-based que leia cada chunk, classifique seu conteúdo conforme a taxonomia controlada (`metadata-taxonomy.yaml`) e reescreva o JSONL enriquecido. Após o enriquecimento, o Chroma deve ser re-indexado com os novos metadados.

**Gap identificado (validação `kb-quality-report.json`):**
- `techniques`: 82% vazio (2.924/3.581 chunks)
- `literary_elements`: ~100% vazio
- `persuasion_drivers`: ~100% vazio
- `confidence_score` médio: 0.52 (baixo — classificação sem LLM)

---

## Scope

### Inclui
- Script `squads/light-copy-kb/scripts/enrich-techniques.js` que processa o JSONL chunk por chunk via Anthropic Claude API
- Prompt few-shot baseado na taxonomia controlada (`metadata-taxonomy.yaml`)
- Atualização do `data/normalized-chunks.jsonl` com campos enriquecidos
- Re-geração do `data/kb-quality-report.json` com métricas pós-enriquecimento
- Re-ingestão no Chroma (`data/chroma/`) com metadados atualizados
- Mecanismo de checkpoint (resume) para não reprocessar chunks já enriquecidos
- Log de progresso com estimativa de custo (tokens/chunks)

### Não Inclui
- Criação do agente de rewrite (S-09.2 — próxima story)
- Alteração do schema `schema-light-copy.json` (os campos já existem, só estão vazios)
- Nova ingestão de transcrições
- Fine-tuning de modelo
- Interface de usuário

---

## Acceptance Criteria

- [ ] **AC1:** Script `enrich-techniques.js` existe em `squads/light-copy-kb/scripts/` e é executável via `node scripts/enrich-techniques.js`
- [ ] **AC2:** Após execução completa, `techniques` preenchido em >= 70% dos chunks
- [ ] **AC3:** Após execução completa, `literary_elements` preenchido em >= 50% dos chunks
- [ ] **AC4:** Após execução completa, `persuasion_drivers` preenchido em >= 40% dos chunks
- [ ] **AC5:** `confidence_score` médio sobe de 0.52 para >= 0.65
- [ ] **AC6:** Script suporta `--resume` para continuar de onde parou sem reprocessar chunks já enriquecidos
- [ ] **AC7:** Todos os valores atribuídos pertencem ao vocabulário controlado de `metadata-taxonomy.yaml` (sem invenção de termos fora da taxonomia)
- [ ] **AC8:** `kb-quality-report.json` é regenerado com métricas pós-enriquecimento
- [ ] **AC9:** Chroma re-indexado com os novos metadados (coleção `light-copy-chunks` atualizada)
- [ ] **AC10:** Log de execução inclui: total chunks, chunks enriquecidos, tokens consumidos, custo estimado, erros

---

## Tasks / Subtasks

### Fase 1: Decisão de Arquitetura (@architect)
- [ ] **T1.1** Definir estratégia de prompt: zero-shot vs. few-shot (3-5 exemplos por técnica recomendado)
- [ ] **T1.2** Definir modelo: claude-haiku-4-5 (custo) vs. claude-sonnet-4-6 (precisão)
- [ ] **T1.3** Definir estratégia de batching: 1 chunk por request vs. N chunks por request
- [ ] **T1.4** Documentar decisão em `.ai/decision-logs/` (formato ADR)

### Fase 2: Implementação do Script (@dev)
- [ ] **T2.1** Criar `scripts/enrich-techniques.js` com:
  - Leitura linha a linha do `data/normalized-chunks.jsonl`
  - Checkpoint em `data/enrich-checkpoint.json` (salva chunk_id do último processado)
  - Flag `--resume` para retomar de checkpoint
  - Flag `--dry-run` para testar sem API calls
  - Flag `--limit N` para processar apenas N chunks (teste)
- [ ] **T2.2** Implementar prompt few-shot com exemplos da taxonomia (`metadata-taxonomy.yaml`)
  - Técnicas: `logic_of_premises`, `magnetic_opening`, `objection_killing`, `curiosity_hook`, `authority_building`, `contrast_positioning`
  - Literary elements: `aforismo`, `antitese`, `anafora`, `hiperbole`, `ironia`, `paradoxo`, `personificacao`, `jogo_de_palavras`, `setup_punchline`, `triade_comica`, `neologismo`, `analogia`
  - Persuasion drivers: `novelty`, `social_proof`, `urgency`, `scarcity`, `anticipation`, `reciprocity`, `authority`, `community`, `recognition`, `enemy_in_common`
- [ ] **T2.3** Implementar validação de output do LLM (garantir que só aceita termos da taxonomia)
- [ ] **T2.4** Atualizar `confidence_score` baseado em: número de técnicas identificadas + coerência com `intent` e `chunk_type`
- [ ] **T2.5** Salvar JSONL enriquecido em `data/normalized-chunks.jsonl` (sobrescrever)
- [ ] **T2.6** Adicionar script `npm run enrich` em `package.json`

### Fase 3: Re-indexação Chroma (@data-engineer)
- [ ] **T3.1** Limpar coleção `light-copy-chunks` no Chroma (delete + recreate)
- [ ] **T3.2** Re-executar `scripts/ingest-transcripts.js` com JSONL enriquecido
- [ ] **T3.3** Verificar que metadados novos (techniques, literary_elements, persuasion_drivers) foram indexados

### Fase 4: Validação de Qualidade (@qa via analyze-kb.js)
- [ ] **T4.1** Executar `node scripts/analyze-kb.js` e gerar novo `kb-quality-report.json`
- [ ] **T4.2** Verificar que todos os ACs de percentual foram atingidos (AC2-AC5)
- [ ] **T4.3** Executar 5 queries de teste temáticas (1 por intent) e verificar que chunks retornados têm técnicas relevantes

---

## Dev Notes

### Arquitetura de Dados Existente

**Schema do chunk** (`squads/light-copy-kb/data/schema-light-copy.json`):
```json
{
  "metadata": {
    "techniques": ["array", "de", "strings"],      // campo a enriquecer
    "literary_elements": ["array"],                 // campo a enriquecer
    "persuasion_drivers": ["array"],                // campo a enriquecer
    "content_types": ["longform"],                  // já preenchido
    "audience_stage": "warm",                       // já preenchido
    "intent": "attention",                          // já preenchido
    "confidence_score": 0.52                        // a atualizar
  }
}
```

**Taxonomia controlada** (`squads/light-copy-kb/data/metadata-taxonomy.yaml`):
- 6 técnicas Light Copy
- 12 elementos literários
- 10 drivers de persuasão

**IMPORTANTE:** O script DEVE validar que todas as strings retornadas pelo LLM estão presentes na taxonomia antes de salvar. Nunca aceitar termos inventados.

### Variáveis de Ambiente Necessárias

```bash
ANTHROPIC_API_KEY=sk-ant-...   # obrigatório — Claude API
```

Adicionar ao `.env` do projeto ou exportar antes de rodar o script.

### Checkpoint Strategy
O arquivo `data/enrich-checkpoint.json` deve ter estrutura:
```json
{
  "last_chunk_id": "m03-premissas-0042",
  "processed_count": 1250,
  "total_count": 3581,
  "started_at": "2026-02-25T...",
  "updated_at": "2026-02-25T..."
}
```

### Estimativa de Custo
- 3.581 chunks × ~800 tokens/chunk (input) = ~2.9M tokens de input
- Com claude-haiku: ~$0.80 total
- Com claude-sonnet: ~$8.70 total
- Recomendação @architect: decidir baseado no trade-off precisão × custo

### Ingest Existente
O script `scripts/ingest-transcripts.js` já existe e faz a indexação no Chroma. A re-ingestão deve reutilizá-lo sem modificação (T3.2). Se precisar de limpeza prévia da coleção, verificar se Chroma Python client tem `collection.delete()` disponível.

### Estrutura de Arquivos
```
squads/light-copy-kb/
├── scripts/
│   ├── normalize-transcripts.js    # existente — não modificar
│   ├── ingest-transcripts.js       # existente — reutilizar para T3.2
│   ├── analyze-kb.js               # existente — executar em T4.1
│   ├── enrich-techniques.js        # NOVO — criar nesta story
│   └── recommend-techniques.js     # existente
├── data/
│   ├── normalized-chunks.jsonl     # atualizar com enriquecimento
│   ├── enrich-checkpoint.json      # NOVO — criado pelo script
│   ├── kb-quality-report.json      # regenerar após enriquecimento
│   ├── metadata-taxonomy.yaml      # referência — não modificar
│   └── chroma/                     # re-indexar após enriquecimento
└── package.json                    # adicionar script "enrich"
```

---

## Definition of Done

- [ ] Script `enrich-techniques.js` existe e funciona com `--dry-run`
- [ ] Execução completa com 3.581 chunks enriquecidos
- [ ] ACs 2-5 validados numericamente via `analyze-kb.js`
- [ ] Chroma re-indexado (coleção `light-copy-chunks` atualizada)
- [ ] `kb-quality-report.json` regenerado com métricas pós-enriquecimento
- [ ] Checkpoint funcional (`--resume` não reprocessa chunks já feitos)
- [ ] Sem termos fora da taxonomia no JSONL final

---

## Change Log

| Data | Agente | Acao |
|------|--------|------|
| 2026-02-25 | @sm (River) | Story criada em EP-09 — novo epic para Squad Light Copy KB |
